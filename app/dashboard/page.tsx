"use client";
import {useEffect,useMemo,useState} from "react";import {Activity,ArrowUpRight,ExternalLink,Flag,RefreshCw,Route,ShieldCheck} from "lucide-react";import {AppShell} from "@/components/app-shell";
type Tx={id:string;reference:string;amount_minor:string;currency:string;status:string;provider:string|null;risk_score:number;customer_reference:string;customer_email:string;authorization_url:string|null;created_at:string};type Me={user:{id:string;email:string};membership:{role:string;merchant:{id:string;name:string;slug:string;status:string;settlement_currency:string}};providers:{configured:string[];paystack:boolean;flutterwave:boolean;monnify:boolean}};
function money(v:string,c:string){return new Intl.NumberFormat("en-NG",{style:"currency",currency:c,maximumFractionDigits:2}).format(Number(v)/100)}
export default function Dashboard(){const [me,setMe]=useState<Me|null>(null),[items,setItems]=useState<Tx[]>([]),[error,setError]=useState(""),[busy,setBusy]=useState(false),[form,setForm]=useState({amount:"25000",currency:"NGN",customerEmail:"",customerReference:"",customerName:"",provider:""});async function load(){setError("");const [mr,tr]=await Promise.all([fetch("/api/v1/me"),fetch("/api/v1/transactions")]);if(mr.status===401){window.location.href="/login";return}const m=await mr.json();const t=await tr.json();if(!mr.ok||!tr.ok){setError(m.error||t.error||"Unable to load dashboard");return}setMe(m);setItems(Array.isArray(t)?t:[])}useEffect(()=>{load()},[]);async function submit(e:React.FormEvent){e.preventDefault();if(!me)return;setBusy(true);setError("");const amountMinor=Math.round(Number(form.amount)*100);const res=await fetch("/api/v1/transactions",{method:"POST",headers:{"content-type":"application/json","idempotency-key":crypto.randomUUID()},body:JSON.stringify({merchantId:me.membership.merchant.id,customerReference:form.customerReference||`customer_${Date.now()}`,customerEmail:form.customerEmail,customerName:form.customerName,amountMinor:String(amountMinor),currency:form.currency,provider:form.provider||undefined})});const data=await res.json();setBusy(false);if(!res.ok){setError(data.error||"Unable to initialize payment");await load();return}await load();if(data.checkout?.authorizationUrl)window.open(data.checkout.authorizationUrl,"_blank","noopener,noreferrer")}
 const metrics=useMemo(()=>({total:items.length,review:items.filter(x=>x.status==="requires_review").length,succeeded:items.filter(x=>x.status==="succeeded").length,volume:items.filter(x=>x.status==="succeeded"&&x.currency==="NGN").reduce((s,x)=>s+Number(x.amount_minor),0)}),[items]);return <AppShell authenticated><main className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><div className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="muted text-sm">{me?.membership.merchant.name||"Merchant workspace"}</p><h1 className="mt-1 text-3xl font-semibold">Payments operations</h1></div><button className="btn btn-secondary" onClick={load}><RefreshCw className="size-4"/>Refresh</button></div>{error&&<div className="mt-5 rounded-xl border border-red-500/20 bg-red-500/10 p-4 text-sm text-red-400">{error}</div>}{me&&me.providers.configured.length===0&&<div className="mt-5 rounded-xl border border-amber-500/20 bg-amber-500/10 p-4 text-sm text-amber-500">No live payment provider credential is configured on the deployment yet. Vortix will not create a fake successful payment; checkout initialization will remain unavailable until at least one provider secret is added.</div>}<div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Transactions",metrics.total,Activity],["Successful",metrics.succeeded,ShieldCheck],["Review queue",metrics.review,Flag],["Successful NGN volume",money(String(metrics.volume),"NGN"),ArrowUpRight]].map(([k,v,I]:any)=><div className="panel p-5" key={k}><div className="flex items-center justify-between"><span className="muted text-sm">{k}</span><I className="size-4 text-blue-400"/></div><div className="mt-4 text-2xl font-semibold">{v}</div></div>)}</div><div className="mt-6 grid gap-6 xl:grid-cols-[.8fr_1.2fr]"><form onSubmit={submit} className="panel p-6"><div className="flex items-center gap-2"><Route className="size-5 text-blue-400"/><h2 className="font-semibold">Initialize live checkout</h2></div><p className="muted mt-2 text-sm">Vortix creates a hosted checkout with a configured provider; card details never pass through this application.</p><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm"><span className="muted">Amount</span><input type="number" min="1" step="0.01" required value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})}/></label><label className="grid gap-2 text-sm"><span className="muted">Currency</span><select value={form.currency} onChange={e=>setForm({...form,currency:e.target.value})}><option>NGN</option><option>USD</option><option>GBP</option><option>EUR</option></select></label><label className="grid gap-2 text-sm sm:col-span-2"><span className="muted">Customer email</span><input type="email" required value={form.customerEmail} onChange={e=>setForm({...form,customerEmail:e.target.value})}/></label><label className="grid gap-2 text-sm"><span className="muted">Customer name</span><input value={form.customerName} onChange={e=>setForm({...form,customerName:e.target.value})}/></label><label className="grid gap-2 text-sm"><span className="muted">Order / customer reference</span><input required value={form.customerReference} onChange={e=>setForm({...form,customerReference:e.target.value})}/></label><label className="grid gap-2 text-sm sm:col-span-2"><span className="muted">Preferred provider</span><select value={form.provider} onChange={e=>setForm({...form,provider:e.target.value})}><option value="">Automatic routing</option>{me?.providers.paystack&&<option value="paystack">Paystack</option>}{me?.providers.flutterwave&&<option value="flutterwave">Flutterwave</option>}{me?.providers.monnify&&<option value="monnify">Monnify</option>}</select></label></div><button disabled={busy||!me||me.providers.configured.length===0} className="btn btn-primary mt-5 w-full">{busy?"Initializing…":"Create checkout"}</button></form><section className="panel overflow-hidden"><div className="border-b p-6" style={{borderColor:"var(--border)"}}><h2 className="font-semibold">Recent transactions</h2><p className="muted mt-1 text-sm">Final success is recorded only after signed webhook and provider verification.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-sm"><thead className="muted text-left"><tr><th className="p-4">Reference</th><th>Amount</th><th>Risk</th><th>Provider</th><th>Status</th><th></th></tr></thead><tbody>{items.map(x=><tr key={x.id} className="border-t" style={{borderColor:"var(--border)"}}><td className="p-4"><div className="font-medium">{x.reference}</div><div className="muted text-xs">{x.customer_email}</div></td><td>{money(x.amount_minor,x.currency)}</td><td>{x.risk_score}</td><td>{x.provider||"—"}</td><td><span className={`badge status-${x.status.toUpperCase()}`}>{x.status.replaceAll("_"," ")}</span></td><td>{x.authorization_url&&x.status!=="succeeded"?<a href={x.authorization_url} target="_blank" rel="noreferrer" title="Open checkout"><ExternalLink className="size-4"/></a>:null}</td></tr>)}{items.length===0&&<tr><td colSpan={6} className="muted p-8 text-center">No payments yet.</td></tr>}</tbody></table></div></section></div></main></AppShell>}
