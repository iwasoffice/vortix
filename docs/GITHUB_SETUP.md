# GitHub setup
For an existing repository, preserve `.git`, create an upgrade branch, replace the working tree with this package and commit. For a new repository, initialise Git, commit, add the remote and push `main`. Never commit `.env` files or signing credentials.
