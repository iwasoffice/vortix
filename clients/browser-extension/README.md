# Vortix browser extension
Manifest V3 operations companion. `chromium/` targets Chrome, Edge and compatible Chromium browsers. `firefox/` contains Firefox metadata. Configure the deployed Vortix URL and optional API key in Settings.
