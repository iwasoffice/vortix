"use client";
import Link from "next/link";import {useState} from "react";import {createClient} from "@/lib/supabase/client";import {AppShell} from "@/components/app-shell";
export default function SignupPage(){const [form,setForm]=useState({fullName:"",companyName:"",email:"",password:""}),[error,setError]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");setMessage("");const origin=window.location.origin;const {data,error}=await createClient().auth.signUp({email:form.email,password:form.password,options:{emailRedirectTo:`${origin}/auth/callback?next=/dashboard`,data:{full_name:form.fullName,company_name:form.companyName}}});setBusy(false);if(error){setError(error.message);return}if(data.session)window.location.href="/dashboard";else setMessage("Account created. Check your email to confirm your address, then sign in.")}return <AppShell><main className="mx-auto max-w-lg px-5 py-14"><div className="panel p-7"><p className="muted text-sm">Commercial merchant account</p><h1 className="mt-2 text-3xl font-semibold">Create your Vortix account</h1><p className="muted mt-3 text-sm leading-6">Your signup creates a private merchant workspace with tenant-isolated payment data.</p><form onSubmit={submit} className="mt-7 grid gap-4"><label className="grid gap-2 text-sm"><span>Full name</span><input required value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></label><label className="grid gap-2 text-sm"><span>Company / merchant name</span><input required value={form.companyName} onChange={e=>setForm({...form,companyName:e.target.value})}/></label><label className="grid gap-2 text-sm"><span>Email</span><input type="email" required autoComplete="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></label><label className="grid gap-2 text-sm"><span>Password</span><input type="password" minLength={8} required autoComplete="new-password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{error&&<div className="rounded-lg border border-red-500/20 bg-red-500/10 p-3 text-sm text-red-400">{error}</div>}{message&&<div className="rounded-lg border border-emerald-500/20 bg-emerald-500/10 p-3 text-sm text-emerald-400">{message}</div>}<button className="btn btn-primary" disabled={busy}>{busy?"Creating account…":"Create account"}</button></form><p className="muted mt-5 text-sm">Already registered? <Link className="text-blue-400" href="/login">Sign in</Link></p></div></main></AppShell>}
