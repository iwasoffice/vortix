const base=process.env.VORTIX_APP_URL?.replace(/\/$/,"");const token=process.env.VORTIX_RECONCILIATION_TOKEN;if(!base||!token){console.error("VORTIX_APP_URL and VORTIX_RECONCILIATION_TOKEN are required");process.exit(1)}const response=await fetch(`${base}/api/internal/reconcile`,{method:"POST",headers:{"x-vortix-reconciliation-token":token}});const body=await response.text();console.log(body);if(!response.ok)process.exit(1)
