import type {CheckoutRequest,CheckoutResult} from "./types";import {ProviderConfigurationError} from "./types";
const endpoint="https://api.flutterwave.com/v3";const timeout=()=>AbortSignal.timeout(15_000);
export function flutterwaveConfigured(){return Boolean(process.env.FLUTTERWAVE_SECRET_KEY)}
function key(){const value=process.env.FLUTTERWAVE_SECRET_KEY;if(!value)throw new ProviderConfigurationError("Flutterwave is not configured");return value}
export async function initializeFlutterwave(input:CheckoutRequest):Promise<CheckoutResult>{const res=await fetch(`${endpoint}/payments`,{method:"POST",signal:timeout(),headers:{Authorization:`Bearer ${key()}`,"Content-Type":"application/json"},body:JSON.stringify({tx_ref:input.reference,amount:Number(input.amountMinor)/100,currency:input.currency,redirect_url:input.callbackUrl,customer:{email:input.customerEmail,name:input.customerName||input.customerEmail},meta:input.metadata??{},customizations:{title:"Vortix payment"}})});const body=await res.json();if(!res.ok||body?.status!=="success"||!body?.data?.link)throw new Error(body?.message||`Flutterwave initialization failed (${res.status})`);return{provider:"flutterwave",providerReference:input.reference,authorizationUrl:String(body.data.link),raw:body}}
export async function verifyFlutterwave(transactionId:string){const res=await fetch(`${endpoint}/transactions/${encodeURIComponent(transactionId)}/verify`,{signal:timeout(),headers:{Authorization:`Bearer ${key()}`}});const body=await res.json();if(!res.ok||body?.status!=="success")throw new Error(body?.message||"Flutterwave verification failed");return body.data}
export async function verifyFlutterwaveByReference(reference:string){const to=new Date(),from=new Date(to.getTime()-7*86400000);const params=new URLSearchParams({from:from.toISOString().slice(0,10),to:to.toISOString().slice(0,10),tx_ref:reference});const res=await fetch(`${endpoint}/transactions?${params}`,{signal:timeout(),headers:{Authorization:`Bearer ${key()}`}});const body=await res.json();if(!res.ok||body?.status!=="success")throw new Error(body?.message||"Flutterwave reference lookup failed");const rows=Array.isArray(body.data)?body.data:[];return rows.find((x:any)=>String(x.tx_ref)===reference)||null}
