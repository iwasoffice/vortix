# Upgrading an existing Git repository
Preserve the `.git` directory. Create an upgrade branch, replace the working tree with this repository, run database migrations and quality checks, then open a pull request. This preserves history, issues, Vercel linkage and repository URLs.
