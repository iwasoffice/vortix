import Link from "next/link";import {AppShell} from "@/components/app-shell";
export default async function PaymentReturn({params}:{params:Promise<{reference:string}>}){const {reference}=await params;return <AppShell><main className="mx-auto max-w-xl px-5 py-20"><div className="panel p-8 text-center"><h1 className="text-3xl font-semibold">Payment received for verification</h1><p className="muted mt-4 leading-7">Reference <strong>{reference}</strong> has returned from the payment provider. Vortix confirms final status server-side from the provider webhook or verification API before the merchant delivers value.</p><Link href="/" className="btn btn-secondary mt-7">Return to Vortix</Link></div></main></AppShell>}
