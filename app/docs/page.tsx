import {AppShell} from "@/components/app-shell";
export default function Docs(){return <AppShell><main className="mx-auto max-w-5xl px-5 py-12 lg:px-8"><p className="muted text-sm">Developer reference</p><h1 className="mt-2 text-4xl font-semibold">Vortix API</h1><p className="muted mt-3 max-w-2xl">Authenticated dashboard calls use the user session. Server-to-server integrations use a Vortix API key in <code>x-vortix-key</code> once the deployment has its server-side Supabase secret configured.</p><div className="mt-8 grid gap-4"><section className="panel p-6"><h2 className="font-semibold">POST /api/v1/transactions</h2><pre className="mt-4 overflow-auto rounded-xl p-4 text-sm" style={{background:"var(--panel2)"}}>{`Headers:\n  Content-Type: application/json\n  Idempotency-Key: order-1042\n\n{\n  "merchantId": "<your merchant UUID>",\n  "customerReference": "order-1042",\n  "customerEmail": "customer@example.com",\n  "amountMinor": "2500000",\n  "currency": "NGN"\n}`}</pre><p className="muted mt-3 text-sm">If a live provider is configured, the response contains a hosted checkout URL. Vortix never accepts raw card data.</p></section><section className="panel p-6"><h2 className="font-semibold">GET /api/v1/transactions</h2><p className="muted mt-2">Lists transactions visible to the authenticated merchant workspace.</p></section><section className="panel p-6"><h2 className="font-semibold">POST /api/webhooks/:provider</h2><p className="muted mt-2">Provider endpoints: <code>paystack</code>, <code>flutterwave</code>, and <code>monnify</code>. Signatures are verified before provider-side transaction verification and settlement.</p></section><section className="panel p-6"><h2 className="font-semibold">OpenAPI</h2><a className="text-blue-400" href="/openapi.yaml">Download the OpenAPI 3.1 specification</a></section></div></main></AppShell>}
