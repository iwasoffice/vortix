# Vortix desktop
Tauri 2 source for Windows, macOS and Linux. Configure real identifiers, icons and signing/notarisation credentials before public releases.

```bash
npm install
npm run tauri dev
npm run tauri build
```
