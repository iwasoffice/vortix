# Extension privacy template
The extension stores the Vortix API base URL and optional API key locally in browser extension storage. It sends those values only to the configured Vortix service when the user requests operations. Before store publication, adapt this statement to the final telemetry, support and legal setup and ensure it matches actual behaviour.
