import {NextResponse} from "next/server";import {createClient} from "@/lib/supabase/server";import {availableProviders} from "@/lib/providers";export async function GET(){let database="reachable";try{const supabase=await createClient();const {error}=await supabase.from("merchants").select("id",{head:true,count:"exact"}).limit(1);if(error&&!/permission|row-level/i.test(error.message))database="error"}catch{database="error"}return NextResponse.json({status:database==="reachable"?"ok":"degraded",database,providers:availableProviders(),time:new Date().toISOString()},{status:database==="reachable"?200:503})}
