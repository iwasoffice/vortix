export type RiskResult={score:number;level:"low"|"medium"|"high"|"critical";reasons:Array<{code:string;weight:number;detail:string}>};
export function assessRisk(amountMinor:bigint,currency:string,metadata:Record<string,unknown>={}):RiskResult{let score=5;const reasons:RiskResult["reasons"]=[];const amount=Number(amountMinor)/100;if(amount>=5_000_000){score+=45;reasons.push({code:"VERY_LARGE_AMOUNT",weight:45,detail:"Amount exceeds high-value review threshold"})}else if(amount>=1_000_000){score+=25;reasons.push({code:"LARGE_AMOUNT",weight:25,detail:"Large transaction amount"})}if(currency!=="NGN"){score+=10;reasons.push({code:"CROSS_BORDER_CURRENCY",weight:10,detail:"Non-NGN transaction"})}if(metadata.ipCountry&&metadata.billingCountry&&metadata.ipCountry!==metadata.billingCountry){score+=20;reasons.push({code:"GEO_MISMATCH",weight:20,detail:"IP and billing countries differ"})}score=Math.min(score,100);return{score,level:score>=85?"critical":score>=70?"high":score>=40?"medium":"low",reasons}}
